INTROSPECTION_CLIENT=issuer
INTROSPECTION_CLIENT_SECRET=secret
SCOPES=openid,pid:sd_jwt_dc,pid:mso_mdoc,diploma,ehic,por:sd_jwt_vc
METADATA_URL=http://localhost:8003/openid/.well-known/openid-credential-issuer
ACCESS_TOKEN_TTL=30
REFRESH_TOKEN_TTL=2592000
VCT_REGISTRY_URL=http://localhost:8097/type-metadata
# Select one authenticator
# Supported values: user-pass-pid | auth-broker
AUTHENTICATOR=user-pass-pid
# Authenticator: user-pass-pid (used only when AUTHENTICATOR=user-pass-pid)
USER_PASS_PID_DEMO_USERNAME=test
USER_PASS_PID_DEMO_PASSWORD=test
# Authenticator: auth-broker (used only when AUTHENTICATOR=auth-broker)
AUTH_BROKER_PROVIDER_URL=
AUTH_BROKER_CLIENT_ID=
AUTH_BROKER_CLIENT_SECRET=
AUTH_BROKER_SCOPE=openid profile email
AUTH_BROKER_REDIRECT_URI=http://localhost:6060/as/interaction/authBroker/callback
